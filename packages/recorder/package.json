{
  "name": "recorder",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "rimraf ./dist/* && tsc --build tsconfig.json",
    "run": "yarn build && cross-env NODE_ENV=developemnt DEBUG=* node dist/index.js"
  },
  "dependencies": {
    "@types/node": "^17.0.8",
    "cross-env": "^7.0.3",
    "ffmpeg-stream": "^0.7.0",
    "naudiodon": "^2.3.6",
    "node-audiorecorder": "^2.2.0",
    "rimraf": "^3.0.2",
    "typescript": "^4.5.4"
  }
}
